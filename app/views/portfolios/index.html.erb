<div class="album text-muted">
  <div class="container">
    <%= link_to 'New Item', new_portfolio_path if logged_in?(:site_admin) %>
    <div class="row <%= "Ssortable" if logged_in?(:site_admin) %>">
      <%= render partial: 'portfolio_item', collection: @portfolio_items %>
    </div>
  </div>
</div>
